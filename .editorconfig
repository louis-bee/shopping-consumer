# EditorConfig 
[*.{js,jsx,ts,tsx,vue}] # 匹配 JS, JSX, TS, TSX, Vue 文件
indent_style = space # 使用空格进行缩进
indent_size = 2 # 缩进大小为 2 个空格
trim_trailing_whitespace = true # 去除每行末尾的空白字符
insert_final_newline = true # 文件末尾添加一个新行
